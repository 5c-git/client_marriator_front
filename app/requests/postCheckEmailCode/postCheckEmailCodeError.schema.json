{
  "$id": "postCheckEmailCodeError",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "postCheckEmailCodeError",
  "type": "object",

  "properties": {
    "status": {
      "type": "string",
      "const": "error"
    },
    "result": {
      "type": "object",

      "properties": {
        "code": {
          "type": "object",

          "properties": {
            "status": {
              "type": "string",
              "enum": ["error", "notExists"]
            }
          },
          "required": ["status"]
        }
      },
      "required": ["code"]
    }
  },
  "required": ["status", "result"]
}
